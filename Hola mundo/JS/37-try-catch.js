'Use Strict'
//try cash e permite definir un bloque de código que se ejecutará, 
//si se produce un error en el bloque
//DecodeURIComponent sirve para decodificar una URL

try{
    var year=2019;
    alert(year);
    var vector=new Array(999999999999);
    
}catch(error){
    console.log(error);
    alert("A ocurrido un error en el codigo");
}
